#include "main.h"
/**
 * _sqrt_recursion - to return the square root of numbers
 * @i: sqr rt of n
 * Return: int
 */
int _sqrt_recursion(int n)
{
	int i;

	if (i * i = n)
	{
		return n;
	}
	else if (i * i > n)
	{
		return (-1);
	}


